<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h.mechanicalengineering">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utilse.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dictionary.dictionaryActivity"
            android:label="@string/dictionary_activity" />
        <activity android:name=".news.newsActivity" />
        <activity android:name=".emp" />
        <activity
            android:name=".lessons.LessonsActivity"
            android:label="@string/titles" />
        <activity
            android:name=".lessons.ShowActivity"
            android:label="" />
        <activity android:name=".news.NewsShowActivity" />
        <activity
            android:name=".ads.Employ_Activity"
            android:label="آگهی ها" />
        <activity android:name=".ads.ShowAd" />
        <activity android:name=".dictionary.Show_Dictionary_Activity"></activity>
    </application>

</manifest>